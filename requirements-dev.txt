# Установить поверх requirements.txt для разработки и проверки перед коммитом:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest>=7.0.0
pytest-asyncio>=0.21.0
ruff>=0.1.0
